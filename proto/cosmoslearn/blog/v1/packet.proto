syntax = "proto3";
package cosmoslearn.blog.v1;

option go_package = "cosmos-learn/x/blog/types";

// BlogPacketData defines the Blog data packet.
message BlogPacketData {
  oneof packet {
    NoData noData = 1;
  }
}

// NoData defines an empty data packet.
message NoData {}
